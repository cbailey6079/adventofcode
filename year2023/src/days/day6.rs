use std::collections::HashMap;
use crate::{utils, Day};

pub struct Day6 {}

impl Day for Day6 {
    fn part1(&self, file: String) -> String {
        let lines = utils::read_lines(format!("./src/files/day6/{file}.txt").as_str());

        "".to_string()
    }

    fn part2(&self, file: String) -> String {
        let lines = utils::read_lines(format!("./src/files/day6/{file}.txt").as_str());

        "".to_string()
    }
}